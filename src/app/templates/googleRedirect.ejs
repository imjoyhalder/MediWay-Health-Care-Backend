
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authenticating...</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .spinner {
            border: 3px solid rgba(0, 0, 0, 0.1);
            border-left-color: #4285F4;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 1s linear infinite;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
    </style>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen font-sans">

    <div class="max-w-md w-full mx-4 p-8 bg-white rounded-2xl shadow-sm border border-gray-100 text-center">
        <div class="mb-6 flex justify-center">
            <svg class="w-12 h-12" viewBox="0 0 24 24">
                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/>
                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.66l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
        </div>

        <div id="auth-content">
            <h1 class="text-xl font-semibold text-gray-800 mb-2">Redirecting to Google</h1>
            <p class="text-gray-500 mb-8 text-sm">Please wait while we connect your account securely.</p>
            
            <div class="flex justify-center">
                <div class="spinner"></div>
            </div>
        </div>
    </div>

<script>
    (
        async () => {
            try {
                const response = await fetch("<%= betterAuthUrl %>/api/auth/sign-in/social", {
                    method : "POST",
                    headers : {
                        "Content-Type" : "application/json"
                    },
                    credentials : "include",
                    body: JSON.stringify({
                        provider : "google",
                        callbackURL: "<%= callbackURL %>"
                    })
                })

                const data = await response.json();

                if(data.url){
                    window.location.href = data.url
                }else{
                    document.body.innerHTML = `
                    <div class="bg-gray-50 flex items-center justify-center min-h-screen p-4">
                        <div class="max-w-md w-full p-8 bg-white rounded-2xl shadow-sm border-t-4 border-red-500 text-center">
                            <p class="text-gray-800 font-medium">Error Occurred While Redirecting To Google. Please Try Again Later.</p>
                            <button onclick="window.location.reload()" class="mt-4 text-blue-600 font-semibold hover:underline">Try Again</button>
                        </div>
                    </div>`
                }
            } catch (error) {
                document.body.innerHTML = `
                <div class="bg-gray-50 flex items-center justify-center min-h-screen p-4">
                    <div class="max-w-md w-full p-8 bg-white rounded-2xl shadow-sm border-t-4 border-red-500 text-center">
                        <p class="text-gray-800 font-medium">Error Occurred While Redirecting To Google. Please Try Again Later.</p>
                        <p class="text-xs text-red-400 mt-2">${error.message}</p>
                        <button onclick="window.location.reload()" class="mt-4 text-blue-600 font-semibold hover:underline">Try Again</button>
                    </div>
                </div>`
            }
        }
    )()
</script>
</body>
</html>