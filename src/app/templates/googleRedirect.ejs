<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Google Login</title>
</head>

<body>
    <div>
        <p>Redirecting To Google.......</p>
    </div>
</body>
<script>
    (
        async () => {
            try {
                const response = await fetch("<%= betterAuthURL%>/api/auth/sign-in/social", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    credentials: 'true',
                    body: JSON.stringify({
                        provider: "google",
                        callbackURL: "<%= callbackURL%>"
                    })
                })

                const data = await response.json()
                if (data.url) {
                    window.location.href = data.url
                }
                else {
                    `<div>
                    <p>Error Occurred While Redirecting To Google, Please Try Again<p/>
                    <p>${error.message}</p>
                    <div/>`
                }
            }
            catch (error) {
                document.body.innerHTML = `<div>
                    <p>Error Occurred While Redirecting To Google, Please Try Again<p/>
                    <p>${error.message}</p>
                    <div/>`
            }
        }
    )()
</script>

</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authenticating with Google</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Smooth fade-in for the UI */
        .fade-in { animation: fadeIn 0.5s ease-out forwards; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        
        /* Spinner animation */
        .spinner {
            border: 3px solid rgba(0, 0, 0, 0.1);
            border-left-color: #4285F4; /* Google Blue */
            border-radius: 50%;
            width: 24px;
            height: 24px;
            animation: spin 1s linear infinite;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
    </style>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen font-sans">

    <div id="status-container" class="max-w-md w-full mx-4 p-8 bg-white rounded-2xl shadow-sm border border-gray-100 text-center fade-in">
        <div class="mb-6 flex justify-center">
            <svg class="w-10 h-10" viewBox="0 0 24 24">
                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/>
                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.66l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
        </div>

        <div id="loading-state">
            <h1 class="text-xl font-semibold text-gray-800 mb-2">Redirecting to Google</h1>
            <p class="text-gray-500 mb-6">Please wait while we securely connect your account.</p>
            <div class="flex justify-center">
                <div class="spinner"></div>
            </div>
        </div>

        <div id="error-state" class="hidden">
            <h1 class="text-xl font-semibold text-red-600 mb-2">Authentication Error</h1>
            <p id="error-message" class="text-gray-500 mb-6 font-mono text-sm bg-red-50 p-3 rounded"></p>
            <button onclick="window.location.reload()" class="bg-gray-900 text-white px-6 py-2 rounded-lg hover:bg-gray-800 transition">
                Try Again
            </button>
        </div>
    </div>

    <script>
        (async () => {
            const loadingState = document.getElementById('loading-state');
            const errorState = document.getElementById('error-state');
            const errorMessage = document.getElementById('error-message');

            try {
                const response = await fetch("<%= betterAuthURL%>/api/auth/sign-in/social", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    credentials: 'include', // Ensure this is a boolean if your backend expects it, though 'true' as string is common in some setups
                    body: JSON.stringify({
                        provider: "google",
                        callbackURL: "<%= callbackURL%>"
                    })
                });

                if (!response.ok) throw new Error(`Server responded with ${response.status}`);

                const data = await response.json();
                
                if (data.url) {
                    console.log(data.url);
                    window.location.href = data.url;
                } else {
                    throw new Error("No redirection URL received from server.");
                }
            } catch (error) {
                loadingState.classList.add('hidden');
                errorState.classList.remove('hidden');
                errorMessage.innerText = error.message;
                console.error("Auth Redirect Error:", error);
            }
        })();
    </script>
</body>
</html>